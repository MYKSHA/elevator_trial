[project]
name = "trial-elevator"
version = "0.1.0"
description = "Elevator control RTL Phinity trial task"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "cocotb>=2.0.1",
    "cocotb-tools>=0.1.0",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
