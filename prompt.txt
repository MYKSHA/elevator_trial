The elevator RTL in sources/ does not correctly implement SPEC.md.

Fix sources/elevator_controller.sv and sources/elevator_group.sv so the test suite passes. Do not modify files under tests/.

Several independent bugs remain. Read SPEC.md, then audit sources against these areas:

elevator_controller.sv
- §3 min-request rescans: trace `skip_ground_floor` from its combinatorial assignment (near the min/max rescan logic) through every `scan_lowest_request(requests, skip_ground)` call. SPEC §3.3/§3.6: floor 0 is never skipped on a downward sweep — if that flag is true while descending, a pending floor-0 cabin call can vanish from `min_request`, especially after new immediate requests merge mid-descent.
- §3–§4 movement, transit buffering, and e-stop side effects may diverge from SPEC §3.4–§3.8 in several places.
- §3.3 stop-at-floor rules apply in motion states too — not only when idle at a floor.

elevator_group.sv — `compute_lift_score` (SPEC §5)
- §5.1 lift availability under alarm conditions.
- §5.2 idle-band vs in-motion scoring weights — compare shift amounts to the §5.2 table; a car moving favorably toward the call should beat a distant idle car.
